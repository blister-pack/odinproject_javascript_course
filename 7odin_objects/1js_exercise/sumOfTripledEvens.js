// the objective of this exercise is to rewrite the function
// using filter, map and reduce

function mapSumOfTripledEvents(params) {
    // func using map
}

function filterSumOfTripledEvents(params) {
    // func using filter
}

function reduceSumofTripledEvents(params) {
    // func using filter
}